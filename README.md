# 🔌 Парсер графіків відключення світла Вінниці

Автоматичний парсер графіків відключення з voe.com.ua для Вінницької області.

**🚀 Запускається автоматично на GitHub Actions кожні 15 хвилин!**

## ⚡ Основні можливості

- ✅ Автоматичний парсинг графіків відключення Вінниці
- ✅ Запускається на серверах GitHub (без потреби у локальному хостингу)
- ✅ Підтримка Playwright для взаємодії з JS-контентом
- ✅ Структурований вивід у JSON формат
- ✅ Збереження історії з unix-timestamps
- ✅ Розпізнавання 7 типів станів світла
- ✅ Розподіл на поточний і наступний дні
- ✅ Докладна інформація про черги відключень

## 📊 Формат JSON

```json
{
  "regionId": "vinnytsia",
  "lastUpdated": "2025-11-25T19:51:21.371Z",
  "fact": {
    "data": {
      "1764021600": {
        "GPV1.1": {
          "1": "yes",
          "2": "yes",
          "3": "first",
          "4": "no",
          "5": "maybe"
        }
      }
    },
    "updateFact": "25.11.2025 19:57"
  },
  "lastUpdateStatus": {
    "status": "parsed",
    "ok": true,
    "code": 200
  }
}
```

## 🔴 Можливі стани

| Стан | Значення | Опис |
|------|----------|------|
| `yes` | ✅ | Світло весь час |
| `no` | ❌ | Немає світла весь час |
| `first` | ⬅️ | Вимкнення перші 30 хв |
| `second` | ➡️ | Вимкнення другі 30 хв |
| `maybe` | ❓ | Можливе вимкнення |

## 📁 Структура проекту

```
vinnytsia-outage-parser/
├── .github/
│   └── workflows/
│       └── parser.yml      # GitHub Actions workflow
├── scripts/
│   ├── parser.js           # Головний парсер
│   ├── regions.js          # Конфігурація регіонів
│   └── utils.js            # Допоміжні функції
├── data/
│   └── vinnytsia.json      # JSON файл з графіками
├── package.json
├── package-lock.json       # Lock файл для npm
├── .env.example            # Приклад конфігурації
├── .gitignore
├── README.md
└── LICENSE
```

## ⏱️ Розклад запусків

Парсер запускатиметься автоматично **кожні 15 хвилин (UTC)**:

```
00:00, 00:15, 00:30, 00:45
01:00, 01:15, 01:30, 01:45
...
23:00, 23:15, 23:30, 23:45
```

**Всього: 96 запусків на добу**

## 📊 Переглянути результати

### На GitHub:

1. Перейдіть на https://github.com/olnet93/vinnytsia-outage-parser
2. Вкладка **Actions** → див логи виконання
3. Файл **data/vinnytsia.json** → див оновлений графік

### Локально (після git pull):

```bash
git pull
cat data/vinnytsia.json | python -m json.tool
```

## 📖 Приклад використання

```javascript
import fs from 'fs';

const data = JSON.parse(
  fs.readFileSync('data/vinnytsia.json', 'utf-8')
);

Object.entries(data.fact.data).forEach(([timestamp, queues]) => {
  const date = new Date(parseInt(timestamp) * 1000);
  console.log(`Дата: ${date.toLocaleDateString('uk-UA')}`);

  Object.entries(queues).forEach(([queueName, hours]) => {
    console.log(`  Черга: ${queueName}`);
    Object.entries(hours).forEach(([hour, state]) => {
      console.log(`    ${hour}:00 - ${state}`);
    });
  });
});
```

## 🔄 Запуск вручну

В вкладці **Actions** на GitHub:
1. Виберіть workflow **"Parse Disconnection Data"**
2. Натисніть **"Run workflow"**
3. Парсер запуститься негайно

## ☁️ GitHub Actions

- ✅ Безкоштовно (до 2000 хв на місяць)
- ✅ Запускається на GitHub серверах
- ✅ Не потребує вашого ПК
- ✅ Автоматичне збереження результатів
- ✅ Історія всіх запусків

## 📝 Ліцензія

MIT License - див. файл LICENSE

---

Made with ❤️ for Vinnytsia 🇺🇦
